Array in Go is a collection of elements of the same data type stored at contiguous memory locations. The size of an array is fixed. E.g: var arrayName [size]dataType

Array store multiple values of the same type in one variable. e.g. var numbers [3]int = [3]int{10, 20, 30}
fmt.Println(numbers[0])  // Output: 10
